<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <FrameLayout
        android:id="@+id/facebook_button"
        android:layout_width="0dp"
        android:layout_height="@dimen/action_button_height"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@drawable/cleeng_login_facebook_button">

        <TextView
            android:id="@+id/sign_in_facebook_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:drawablePadding="14dp"
            android:drawableStart="@drawable/cleeng_login_facebook_button_icon"
            android:gravity="center"
            tools:text="Sign In with Facebook" />

    </FrameLayout>

    <FrameLayout
        android:id="@+id/google_button"
        android:layout_width="0dp"
        android:layout_height="@dimen/action_button_height"
        android:layout_marginTop="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/facebook_button"
        android:background="@color/red">

        <TextView
            android:id="@+id/sign_in_google_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:drawablePadding="14dp"
            android:drawableStart="@drawable/googleg_standard_color_18"
            android:gravity="center"
            tools:text="Sign In with Google" />

    </FrameLayout>
</android.support.constraint.ConstraintLayout>